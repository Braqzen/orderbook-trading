syntax = "proto3";

package generatorfeed;

import "google/protobuf/empty.proto";

service GeneratorFeed {
  rpc PublishPrice (stream PriceUpdate) returns (google.protobuf.Empty);
}

message PriceUpdate {
  string instrument = 1;
  double value = 2;
}
